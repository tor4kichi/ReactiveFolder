<UserControl x:Class="Modules.Main.Views.ReactionEditer.ActionEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Modules.Main.Views"
             xmlns:vm="clr-namespace:Modules.Main.ViewModels"
             xmlns:editervm="clr-namespace:Modules.Main.ViewModels.ReactionEditer"
             xmlns:editerView="clr-namespace:Modules.Main.Views.ReactionEditer"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid x:Name="ActionsContainer" >
        <StackPanel Orientation="Vertical">

            <Grid >
                <TextBlock Text="Actions" 
                       Style="{StaticResource ListSubheaderText}"
                       >

                </TextBlock>

                <Button Command="{Binding AddAppLaunchActionCommand}"
                     ToolTip="Add AppLaunchAction" 
                    Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                    HorizontalAlignment="Right"
                    Margin="16 0"
                    >
                    <materialDesign:PackIcon Kind="Plus">

                    </materialDesign:PackIcon>
                </Button>
            </Grid>

            <ItemsControl ItemsSource="{Binding Actions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="48" />
                            </Grid.ColumnDefinitions>
                            
                            <Button Grid.Column="0"
                                    Command="{Binding Path=DataContext.EditAppLaunchActionCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                                    CommandParameter="{Binding}"
                                    >
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding AppName.Value}" />
                                    <TextBlock Text="{Binding ArgumentName.Value}"
                                               Margin="8 0"
                                               FontSize="12"
                                               />
                                </StackPanel>
                            </Button>

                            <Button Grid.Column="1"
                                    Command="{Binding RemoveActionCommand}"
                                    >
                                <materialDesign:PackIcon Kind="Delete"></materialDesign:PackIcon>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>


        </StackPanel>
        <materialDesign:DialogHost Identifier="AppLuanchActionEditDialog"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Margin="8 40"
                                       />

    </Grid>

</UserControl>
